import React from 'react';

const Header: React.FC<{}> = () => {
    return (
        <div className='header'>
            <p>Welcome to the React Redux with Hooks example!</p>
        </div>
    );
};

export default Header;
